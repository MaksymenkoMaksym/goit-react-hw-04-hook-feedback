import styled from 'styled-components';

export const FeedbackListStyled = styled.ul`
  list-style: none;
  margin: 0;
  padding: 0;
`;

export const FeedbackItem = styled.li`
  text-transform: uppercase;
  font-size: 30px;
`;
